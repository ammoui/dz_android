# Jikan Explorer

## ФИО и группа

- ФИО: Манычкин Артём Евгеньевич
- Группа: Б9124-09.03.03пикд(4)

## Выбранный API

- Jikan API v4: https://api.jikan.moe/v4/

## В Room

- Таблица: `favourites` (FavouriteEntity)
- Поля: `key`, `mediaType`, `mediaId`, `title`
- Добавление и удаление избранного на экране деталей + список избранного отображается на экране списка

## Тестирование (ДЗ 5)

- **Юнит-тесты**: 19 (покрывают загрузку, ошибки, retry, начальные состояния).
- **Интеграционные тесты**: 15 (интеграция Repository+Room, а также UI-тест `MediaListIntegrationTest` на сценарий "ошибка -> нажатие Retry -> успешное состояние").
- **Сценарии покрыты**:
  - Успешная загрузка данных, обработка ошибок, retry() механизм.
  - Сохранение и предотвращение дублей в избранном (`FavouriteToggleBehaviorTest`).
  - Фоллбек на локальную БД при недоступности API (`offline detail loading falls back to Room`).
- **Flow тесты**: 3. Написаны тесты с использованием библиотеки Turbine (`JikanRepositoryFlowTest`). Проверена полная последовательность эмиссий при изменении списка избранного и нетривиальное потоковое поведение (отсутствие дублирующих эмиссий при одинаковых данных).
